import Link from 'next/link';
import { entries } from '../../content/reader';
export const metadata={title:'作品案例'};
export default function Page(){const covers:Record<string,string>={'ethereal-ruins':'ruins-mid.webp','gameplay-polish':'polish-after.webp','christmas-snowball':'snow-overview.webp','penetration-system':'penetration-layout.webp'};const items=entries('work');const groups=[{title:'核心案例',intro:'原创地图、既有地图诊断与限时模式交付。',slugs:['ethereal-ruins','gameplay-polish','christmas-snowball']},{title:'相关设计实践',intro:'补充系统与空间协作、多模式白盒及早期项目经验。',slugs:['penetration-system','combat-maps','longtu-battle-royale']}];return <section className="page section"><p className="eyebrow">SELECTED WORK</p><h1>作品案例<span>。</span></h1><p className="page-intro">以射击游戏地图为核心，沿着设计目标、空间决策与制作交付阅读我的工作。</p>{groups.map(g=><section className="work-group" key={g.title}><h2>{g.title}</h2><p className="page-intro">{g.intro}</p><div className="content-grid">{g.slugs.map(slug=>{const e=items.find(x=>x.slug===slug);return e?<Link className="content-card" key={e.slug} href={'/work/'+e.slug}>{covers[e.slug]&&<img className="card-media" src={(process.env.NEXT_PUBLIC_BASE_PATH||'')+'/media/work/'+covers[e.slug]} alt={e.title} loading="lazy"/>}<h3>{e.title}</h3><p>{e.summary}</p><span>阅读案例 →</span></Link>:null})}</div></section>)}<Link className="text-link" href="/work/systems-iteration">其他工作：热力图迭代与训练场评分 →</Link></section>}
